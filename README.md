# Email & Calendar agent
A Python automation that reads your Gmail inbox, extracts event details using an LLM, and sends a Telegram message with ✅ Approve / ❌ Skip buttons. Calendar events are only created after your explicit approval.
